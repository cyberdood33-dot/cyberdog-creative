import { CyberdogShell, Eyebrow } from "@/components/CyberdogShell";
import { useAuth } from "@/_core/hooks/useAuth";
import { useBootstrap } from "@/contexts/BootstrapContext";
import { BarChart3, Eye, Gauge, LockKeyhole, MousePointer2 } from "lucide-react";

export default function Analytics(){const{user,isAuthenticated}=useAuth();const{portfolio,posts,feed,docs}=useBootstrap();if(!isAuthenticated||user?.role!=="admin")return <CyberdogShell><section className="container grid min-h-[60vh] place-items-center py-16 text-center"><div><LockKeyhole className="mx-auto size-8 text-[#c92020]"/><h1 className="mt-4 font-display text-4xl font-extrabold">Owner analytics only.</h1><p className="mt-3 max-w-md text-sm leading-6 text-zinc-600">Traffic reporting is intentionally limited to the Cyberdog Creative site owner.</p></div></section></CyberdogShell>;return <CyberdogShell><section className="container py-12"><Eyebrow>Owner desk / Analytics</Eyebrow><div className="mt-4 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><h1 className="font-display text-5xl font-extrabold tracking-[-.07em]">Read the <span className="text-[#c92020]">signal.</span></h1><p className="mt-3 max-w-2xl text-sm leading-6 text-zinc-600">Cyberdog Creative uses the installed privacy-conscious analytics service for aggregate site traffic. Open the project dashboard for live visitor and page-view reporting; this page focuses on real content readiness rather than invented traffic figures.</p></div><div className="flex flex-wrap gap-2"><a href="/automation" className="min-h-11 rounded-lg bg-[#c92020] px-4 py-2.5 text-sm font-extrabold text-white shadow-sm">Open FastAPI workflow lab</a><a href="/admin" className="min-h-11 rounded-lg border border-zinc-200 bg-white px-4 py-2.5 text-sm font-extrabold text-zinc-700 shadow-sm">Back to owner desk</a></div></div><div className="mt-9 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Metric icon={Gauge} label="Portfolio case files" value={portfolio.length}/><Metric icon={Eye} label="Published journal entries" value={posts.length}/><Metric icon={MousePointer2} label="Social updates" value={feed.length}/><Metric icon={BarChart3} label="Documentation pages" value={docs.length}/></div><div className="mt-8 rounded-2xl border border-zinc-200 bg-white p-6 shadow-sm"><h2 className="font-display text-2xl font-extrabold tracking-[-.045em]">Privacy posture</h2><div className="mt-5 grid gap-4 md:grid-cols-3"><Detail title="Aggregate by design" body="Use site-level traffic and page performance trends to improve journeys, not to profile individual members."/><Detail title="No private-message analytics" body="Encrypted private messages and their content remain outside both AI and analytics surfaces."/><Detail title="Purpose before collection" body="Add events only when they explain a user journey or reveal a real improvement opportunity."/></div></div></section></CyberdogShell>}
function Metric({icon:Icon,label,value}:{icon:any,label:string,value:number}){return <div className="rounded-xl border border-zinc-200 bg-white p-5 shadow-sm"><Icon className="size-5 text-[#c92020]"/><p className="mt-6 font-display text-4xl font-extrabold tracking-[-.06em]">{value}</p><p className="mt-1 text-xs font-extrabold uppercase tracking-[.13em] text-zinc-500">{label}</p></div>}
function Detail({title,body}:{title:string,body:string}){return <div className="rounded-xl bg-[#f7f5f1] p-5"><h3 className="text-sm font-extrabold">{title}</h3><p className="mt-2 text-sm leading-6 text-zinc-600">{body}</p></div>}
