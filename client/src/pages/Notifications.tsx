import { CyberdogShell, Eyebrow } from "@/components/CyberdogShell";
import { NotificationControls } from "@/components/NotificationControls";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { BellRing, CheckCheck, ChevronRight, LockKeyhole, Megaphone, Settings2 } from "lucide-react";
import { Link } from "wouter";
import { toast } from "sonner";

const labels={system:"System",comment:"Comment",follow:"Follow",friend:"Friend",support:"Support",release:"Release"} as const;

export default function Notifications(){const{user,isAuthenticated}=useAuth();const{data=[],refetch,isLoading}=trpc.notifications.inbox.useQuery(undefined,{enabled:isAuthenticated});const markRead=trpc.notifications.markRead.useMutation({onSuccess:()=>refetch(),onError:error=>toast.error(error.message)});if(!isAuthenticated)return <CyberdogShell><section className="container grid min-h-[60vh] place-items-center py-16 text-center"><div><LockKeyhole className="mx-auto size-8 text-[#c92020]"/><h1 className="mt-4 font-display text-4xl font-extrabold">Your notification center is waiting.</h1><p className="mt-3 max-w-md text-sm leading-6 text-zinc-600">Sign in to review account, community, and product signals tailored to your Cyberdog experience.</p></div></section></CyberdogShell>;const unread=data.filter(notification=>!notification.readAt);return <CyberdogShell><section className="container py-12"><Eyebrow>account.cyberdog.io / Notifications</Eyebrow><div className="mt-4 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="font-display text-5xl font-extrabold tracking-[-.07em]">Keep the <span className="text-[#c92020]">signal.</span></h1><p className="mt-3 max-w-2xl text-sm leading-6 text-zinc-600">In-app notifications stay inside Cyberdog Creative. Browser delivery is optional and remains subject to the permission setting on your device.</p></div><div className="flex flex-wrap gap-2"><Link href="/account#notifications" className="inline-flex min-h-11 items-center gap-2 rounded-lg border border-zinc-200 bg-white px-4 py-2.5 text-sm font-extrabold text-zinc-700"><Settings2 className="size-4"/>Preferences</Link>{user?.role==="admin"&&<Link href="/notify" className="inline-flex min-h-11 items-center gap-2 rounded-lg bg-[#c92020] px-4 py-2.5 text-sm font-extrabold text-white"><Megaphone className="size-4"/>Create notice</Link>}</div></div><div className="mt-8 flex items-center justify-between rounded-xl border border-zinc-200 bg-white p-4 shadow-sm"><div><p className="text-sm font-extrabold">{unread.length} unread {unread.length===1?"signal":"signals"}</p><p className="mt-1 text-xs text-zinc-500">Marked messages remain available in this in-app inbox.</p></div><button onClick={()=>markRead.mutate()} disabled={!unread.length||markRead.isPending} className="inline-flex min-h-11 items-center gap-2 rounded-lg bg-zinc-950 px-4 py-2 text-sm font-extrabold text-white disabled:opacity-50"><CheckCheck className="size-4"/>Mark all read</button></div><NotificationControls/><div className="mt-5 space-y-3">{isLoading?<div className="rounded-xl border border-dashed border-zinc-200 p-8 text-sm text-zinc-500">Loading your signals…</div>:data.length===0?<div className="rounded-2xl border border-dashed border-zinc-200 bg-white p-10 text-center"><BellRing className="mx-auto size-8 text-[#c92020]"/><h2 className="mt-4 font-display text-2xl font-extrabold">No notifications yet.</h2><p className="mt-2 text-sm leading-6 text-zinc-600">When someone interacts with your Cyberdog experience or the studio shares an update, it will arrive here.</p></div>:data.map(notification=><article key={notification.id} className={`rounded-2xl border p-5 shadow-sm transition ${notification.readAt?"border-zinc-200 bg-white":"border-red-200 bg-red-50/50"}`}><div className="flex gap-4"><span className={`mt-0.5 size-2 shrink-0 rounded-full ${notification.readAt?"bg-zinc-300":"bg-[#c92020]"}`}/><div className="min-w-0 flex-1"><p className="text-[11px] font-extrabold uppercase tracking-[.15em] text-[#c92020]">{labels[notification.type]}</p><h2 className="mt-1 text-base font-extrabold">{notification.title}</h2><p className="mt-2 text-sm leading-6 text-zinc-600">{notification.body}</p><div className="mt-4 flex flex-wrap items-center gap-3 text-xs text-zinc-500"><span>{new Date(notification.createdAt).toLocaleString()}</span>{notification.href&&<Link href={notification.href} onClick={()=>{if(!notification.readAt)markRead.mutate({ids:[notification.id]})}} className="inline-flex items-center gap-1 font-extrabold text-[#c92020]">Open destination<ChevronRight className="size-3.5"/></Link>}{!notification.readAt&&<button onClick={()=>markRead.mutate({ids:[notification.id]})} className="font-extrabold text-zinc-700 underline underline-offset-4">Mark read</button>}</div></div></div></article>)}</div></section></CyberdogShell>}
